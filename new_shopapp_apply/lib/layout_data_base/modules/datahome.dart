import 'package:flutter/material.dart';

class DataHomeScreen extends StatelessWidget {
  const DataHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container
      (
        child: Text('HomeDataBase'),
      ),
    );
  }
}