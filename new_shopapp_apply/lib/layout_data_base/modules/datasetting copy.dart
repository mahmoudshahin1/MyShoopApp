import 'package:flutter/material.dart';

class Settinsdata extends StatelessWidget {
  const Settinsdata({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container
      (
        child: Text('Settings'),
      ),
    );
  }
}